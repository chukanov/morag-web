# Зависимости инструментов видео (slides_from_video.py, describe_slides.py, screen_refs.py).
# Отдельное окружение, не app/requirements.txt: веб-морде numpy и scipy не нужны, а
# ~/asr-stack/venvs/adaptor принадлежит морагу и не наш.
#
#   python3 -m venv ~/asr-stack/video-venv
#   ~/asr-stack/video-venv/bin/pip install -r tools/requirements-video.txt
#
# Версии прибиты по той же причине, что у адаптера (CLAUDE.md, «Ноутбук — эталон»):
# другая numpy/scipy — другие пороги на границе округления, и шкала слайдов разъедется молча.
# ⚠️ pyyaml — не «на всякий случай»: `upload.py` → `make_record` → `spaces` читает конфиги
# корпуса. На ноутбуке разработчика он оказывается в окружении сам, и пропажа всплыла
# только на ЧИСТОЙ установке с зеркала: приложение не стартовало вовсе.
pyyaml==6.0.3
numpy==2.5.3
scipy==1.18.1
pillow==12.3.0
httpx==0.28.1
# Проверка TLS хранилищем САМОЙ МАШИНЫ: сертификат корпоративного сайта подписан
# внутренним центром сертификации, которого нет в публичных корнях certifi.
truststore==0.10.4
# `av` — это ffmpeg библиотекой: им читаются свойства видео там, где `ffprobe` не
# установлен (маки коллег: с зеркала приезжает только статический `ffmpeg`).
av==14.1.0
pytest==9.1.1
