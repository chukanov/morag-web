"""Вход на сайт: форма + cookie, каталог AD/LDAP и локальные пользователи, роли.

Устройство (каждый модуль — один вопрос, чтобы код входа читался целиком за один присест):

- `roles.py`    — три роли и таблица прав; единственное место, где сказано, кому что можно.
- `local.py`    — пользователи из конфига, пароль хэшем scrypt (stdlib).
- `ldap.py`     — каталог: search-then-bind сервисной учёткой; единственный модуль с `ldap3`.
- `session.py`  — подписанная cookie (HMAC, stdlib), продление, правило `Secure`.
- `store.py`    — снимок учётки на диске: что мы знаем о вошедшем; снимок = сессия существует.
- `throttle.py` — лимит попыток входа (бережём доменную учётку от блокировки).
- `service.py`  — склейка: вход, кто в запросе, cookie, права, подпись правок и сам gate.

Чего здесь НЕТ намеренно: пароль человека нигде не хранится и не логируется; `WWW-Authenticate`
не отдаётся никогда (прокси-помощники у коллег ловят 401-челлендж и уводят вход в цикл);
доменных имён и адресов в коде нет — они в `app/config.yml`.
"""

from .service import AuthService, Identity, gate  # noqa: F401
